name: church_theatre_app
description: "تطبيق كامل وفخم لإدارة أعمال فريق المسرح الكنسي - مسرح حي 2026"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  intl: any
  uuid: ^4.3.3
  file_picker: ^8.0.0
  syncfusion_flutter_pdfviewer: ^28.2.3
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
 # assets:
  #  - assets/
